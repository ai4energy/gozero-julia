package generator

import (
	"fmt"
)

func GenerateAll() error {
	fmt.Println("Generating...") // 输出生成中的提示信息
	// 这里可以添加生成代码的逻辑
	// 假设生成过程中可能发生错误，可以在这里处理这些错误

	// 目前只是示例，没有错误发生
	return nil // 返回 nil 表示没有错误
}
